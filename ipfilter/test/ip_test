#!/bin/sh
testitem=$1
TESTMODE=$2

. ./ipflib.sh

test_init

if [ ${TESTMODE} != user ] ; then
	exit 0
fi
. ./setup_test.sh
echo "${todo}...";
/bin/cp /dev/null ${results}
../ippool -f ${regress} -nRv 2>/dev/null > ${results}
check_results
exit $status
